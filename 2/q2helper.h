#ifndef Q2HELPER_H
#define Q2HELPER_H

#include <uPRNG.h>

// intmax_t convert( const char * str );			// convert C string to integer
inline constexpr int DEFAULT_GAMES = 5;
struct cmd_err{};
struct seed_cmd_err{};
struct game_cmd_err{};
struct player_cmd_err{};

#endif